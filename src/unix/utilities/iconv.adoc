= `iconv`

The `iconv` command converts text between character encodings.

.Example: convert a file from one encoding to another
----
iconv -f ISO-8859-1 -t UTF-8 input.txt > output.txt
----

.Example: list all supported encodings
----
iconv -l
----
